<Window x:Class="KeyboardStop.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="KeyboardStop - 游戏输入法锁定工具" 
        Height="1000" Width="520"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        MinHeight="500" MinWidth="450"
        Background="#F5F5F5"
        StateChanged="Window_StateChanged"
        Closing="Window_Closing">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,10">
            <TextBlock Text="🎮 KeyboardStop" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Foreground="#2196F3"/>
            <TextBlock Text="游戏输入法锁定工具" FontSize="12" 
                       HorizontalAlignment="Center" Foreground="#757575"/>
        </StackPanel>

        <!-- 状态显示 -->
        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="12" Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="StatusIcon" Text="🔓" FontSize="48" VerticalAlignment="Center"/>
                    <StackPanel Margin="15,0,0,0" VerticalAlignment="Center">
                        <TextBlock x:Name="StatusText" Text="当前状态：已解锁" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock x:Name="StatusDesc" Text="输入法可自由切换" FontSize="12" Foreground="#757575"/>
                    </StackPanel>
                </StackPanel>
                
                <Button x:Name="ToggleButton" Content="🔒 锁定英文输入" 
                        FontSize="14" Padding="15,8" Margin="0,10,0,0"
                        Background="#2196F3" Foreground="White" BorderThickness="0"
                        Cursor="Hand" Click="ToggleButton_Click"/>
            </StackPanel>
        </Border>

        <!-- 快捷键配置 -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <TextBlock Text="⌨️ 快捷键配置" FontWeight="SemiBold" FontSize="14"/>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="切换快捷键：" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" x:Name="HotkeyTextBox" 
                             IsReadOnly="True" 
                             Text="Ctrl + Alt + K"
                             Margin="10,0"
                             Padding="10,6"
                             Background="#E3F2FD"
                             BorderThickness="2"
                             BorderBrush="#2196F3"
                             FontFamily="Consolas"
                             FontSize="14"
                             TextAlignment="Center"
                             PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"
                             GotFocus="HotkeyTextBox_GotFocus"
                             LostFocus="HotkeyTextBox_LostFocus"/>
                    <Button Grid.Column="2" x:Name="SaveHotkeyButton" 
                            Content="保存" Padding="15,6"
                            Background="#4CAF50" Foreground="White" BorderThickness="0"
                            Click="SaveHotkey_Click"/>
                </Grid>
                <TextBlock Text="💡 点击输入框后按下新的快捷键组合" 
                           Foreground="#757575" FontSize="10" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- 功能区域 -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="10">
            <StackPanel>
                <TextBlock Text="功能设置" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,5"/>
                
                <CheckBox x:Name="CorrectionCheckBox" Content="启用纠偏轮询" 
                          IsChecked="True" Margin="0,3"/>
                <CheckBox x:Name="StartupCheckBox" Content="开机自动启动" Margin="0,3"/>
                <CheckBox x:Name="MinimizeToTrayCheckBox" Content="最小化到系统托盘" 
                          IsChecked="True" Margin="0,3"/>
                
                <Border Background="#FFF3E0" CornerRadius="4" Padding="8" Margin="0,8,0,0">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚠️ US键盘布局：" VerticalAlignment="Center" FontSize="12" FontWeight="SemiBold"/>
                            <TextBlock x:Name="LayoutStatus" Text="检测中..." VerticalAlignment="Center" Margin="3,0" FontSize="12"/>
                            <Button x:Name="AddLayoutButton" Content="一键添加" Padding="8,2" 
                                    Margin="8,0,0,0" Click="AddLayout_Click" FontSize="11"
                                    Background="#FF9800" Foreground="White" BorderThickness="0"/>
                        </StackPanel>
                        <TextBlock Text="💡 必须先安装 US 英文键盘布局，锁定功能才能正常工作！" 
                                   FontSize="10" Foreground="#E65100" Margin="0,5,0,0" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- 底部广告信息 -->
        <Border Grid.Row="4" Background="#E3F2FD" CornerRadius="8" Padding="12" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Text="🔥 关注作者" FontSize="13" FontWeight="Bold" 
                           HorizontalAlignment="Center" Foreground="#1565C0" Margin="0,0,0,8"/>
                
                <!-- 社交链接横向排列 -->
                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- B站链接 -->
                    <Border Grid.Column="0" Background="White" CornerRadius="6" Padding="8" Margin="0,0,5,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📺 B站" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            <TextBlock HorizontalAlignment="Center" FontSize="10">
                                <Hyperlink NavigateUri="https://space.bilibili.com/436132931" 
                                           RequestNavigate="Hyperlink_RequestNavigate"
                                           Foreground="#FB7299">
                                    436132931
                                </Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                    
                    <!-- Telegram链接 -->
                    <Border Grid.Column="1" Background="White" CornerRadius="6" Padding="8" Margin="5,0,0,0">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="✈️ Telegram" FontSize="12" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            <TextBlock HorizontalAlignment="Center" FontSize="10">
                                <Hyperlink NavigateUri="https://t.me/+RZMe7fnvvUg1OWJl" 
                                           RequestNavigate="Hyperlink_RequestNavigate"
                                           Foreground="#0088CC">
                                    点击加入
                                </Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </Border>
                </Grid>
                
                <!-- 微信赞赏码 -->
                <Border Background="White" CornerRadius="6" Padding="10">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="💰 微信赞赏" FontSize="13" FontWeight="Bold" 
                                   HorizontalAlignment="Center" Foreground="#07C160" Margin="0,0,0,8"/>
                        <Image Source="/Resources/wechat_qr.jpg" Width="220" Height="220" 
                               Stretch="Uniform" HorizontalAlignment="Center"/>
                        <TextBlock Text="感谢您的支持！" FontSize="11" Foreground="#757575" 
                                   HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <TextBlock Text="v1.0.0 | 最小化后可通过系统托盘访问" 
                           HorizontalAlignment="Center" Foreground="#9E9E9E" 
                           FontSize="10" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
